# CONSTITUTION: READ INFRASTRUCTURE 
**Date:** 2026-07-07
**Status:** FROZEN 🔒

---

## Core Principles (Nguyên lý Cốt lõi)

1. Read Infrastructure converts structured tabular data into named objects using column headers.
2. Engines consume named objects only, never positional column indexes.
3. Column order is mutable. Column names are the contract.
4. ```
Read InfrastructureCore PrinciplesRead Infrastructure converts structured tabular data into named objects.Engines consume named objects only.Column order is mutable.Column names are the contract.
```