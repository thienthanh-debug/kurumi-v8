# CHECKPOINT — bắt đầu 2026-07-19, cập nhật đến 2026-08-09

Bản hợp nhất, thay thế vai trò "checkpoint đang sống" của [[CHECKPOINT-2026-07-19]] (giữ file đó làm mốc lịch sử, không xoá/không sửa). File này tự đầy đủ — không cần đọc chéo 07-19 để hiểu bối cảnh.

Liên quan: [[CHECKPOINT-2026-07-19]], [[CHECKPOINT-CHOT-PHIEN-2026-07-14]], [[WASTE-ENGINE-CHECKPOINT-2026-07-22]], [[D7C-PREP-INVENTORY-CHECKPOINT-2026-08-07]], [[D7C3-WASTE-BUTTON-ORCHESTRATION-CHECKPOINT-2026-08-09]], [[ADR-016 Compiler Never Silences Reality]], [[ADR-017 Snapshot ID Format]], [[ADR-018 Waste Event ID Format]], [[ADR-022 Log as Input Buffer with Leader-Triggered Orchestration]], [[Validation-Decision-Spec-V1]], [[Production-Compiler-Execution-Type-Spec-V1]], [[Flag — SessionRAM Persistence across WAIT_REVIEW]], [[Flag — Compiler Automation vs Frozen Contract]], [[Discovery — Snapshot Metadata Ownership]], [[Discovery — Boundary Formula vs Engine]], [[Discovery — Truth Mapping V1]], [[Discovery — Hierarchy of Prevention]]

---

## I. Trạng thái đã đóng, không cần động lại

- Validation Engine (Layer 2) — Frozen. Coverage Matrix 9/9 PASS. [[Validation-Decision-Spec-V1]] Frozen.
- ADR-016 — Compiler Never Silences Reality — Accepted. (PREP_COMPILER không lọc bỏ batch non-OK trước khi vào Staging.)
- Governance Protocol (4 Rule) — `01-GOVERNANCE/`: khuôn Flag 4 câu hỏi, No Consumer→No Field, Knowledge Creation Rule, Open Flag ≠ Freeze Blocker (tiêu chí: rủi ro trong phạm vi Mission đang Freeze mới là Blocker).
- READ-INF-PILOT — Hoàn thành, đóng Mission. Boundary Adapter (`ReadInfrastructure.readAsObjects()` + Domain Adapter) đã thay hẳn cách đọc `row[index]` trong `processGenerateState()`. Dual Read đã xóa sau khi PASS. Kết luận: Validate là tầng Domain sạch, không cần Boundary; Generate là ranh giới Physical/Domain thật.
- `Discovery — Hierarchy of Prevention.md`, `Production-Compiler-Execution-Type-Spec-V1.md` — **đã commit** (`18f4442`, "Production Compiler PASS + Checkpoint 2026-07-19 — Roaster Cashew dropped theo xác nhận Thanh"). Không còn ở trạng thái chờ.
- ✅ Correction đã áp dụng đúng trong vault (verify qua `grep`): case "Roaster Cashew" thuộc [[Discovery — Hierarchy of Prevention]] (`PREP_LOG` bị sửa sau khi Generate nhưng trước khi Commit, khiến Ledger không khớp Reality hiện tại). Discovery "Compiler Automation" **không** chứa tham chiếu này — sạch, không cần sửa gì thêm.

### ✅ FREEZE — Snapshot ID Format (2026-07-22)

Xem đầy đủ tại [[ADR-017 Snapshot ID Format]]. Tóm tắt:

```
Principle: 1 Event = 1 Snapshot = 1 Immutable Transaction Truth
Format: SNP-{EventID}  (ví dụ: DN-ASSM-260612-001 → SNP-DN-ASSM-260612-001)
Rule 01 — Generate Once
Rule 02 — Immutable (không đổi sau Approval/Reject/Rollback/Execution State change)
Rule 03 — One-to-One (1 Event ↔ 1 Snapshot)
Rule 04 — Reference Key (khóa liên kết TRANSACTION_STAGING/INVENTORY_LEDGER/STOCK_POSITION/ERROR_LOG/AUDIT)
Rule 05 — Human Readable (không UUID)
Future Boundary: Snapshot Versioning (1 Event → nhiều Snapshot V1/V2/V3) là Mission riêng, chỉ mở khi Reality cần.
```

Lợi ích phụ: không cần Counter/LockService — mượn tính duy nhất từ Event ID, giảm bớt lý do cần Apps Script Counter riêng ở [[Discovery — Boundary Formula vs Engine]].

⚠️ Phụ thuộc chưa giải quyết (không chặn Freeze này): tính duy nhất của `SNP-{EventID}` phụ thuộc hoàn toàn vào Event ID đã unique. Event ID hiện đang nhập tay — rủi ro trùng Event ID (2 người cùng ngày) vẫn tồn tại ở tầng tạo Event ID, không phải rủi ro mới sinh từ Freeze này.

**Chỉ giải quyết 1/4 field.** 3 field còn lại (`Compiler Source`, `Generated By`, `Generated Time`) vẫn Open — xem [[Discovery — Snapshot Metadata Ownership]].

**🔴 Reality Conflict (2026-07-27) — đã đóng, không còn treo:** trong lúc thiết kế "Stable Waste Event ID" cho Waste Engine (xem [[WASTE-ENGINE-CHECKPOINT-2026-07-22]]), có đề xuất dùng UUID (`WST-550e8400-e29b-...`) để đảm bảo Immutable/Unique tuyệt đối — mâu thuẫn trực tiếp Rule 05. **Quyết định của Thanh:** UUID ❌ không approve; Human-readable static Event ID được chọn — `<Branch Prefix>-WST-<YYMMDD>-<Daily Sequence>` (VD: `DN-WST-260727-014`), sinh qua Apps Script + LockService/counter atomic. Đã formalize thành [[ADR-018 Waste Event ID Format]].

🔴🔴 **Cảnh báo đã xảy ra, cần nhớ lại khi review code:** ngay sau quyết định trên, "Implementation Engine" từng tuyên bố *"FREEZE hoàn toàn"* nhưng mô tả UUID — đúng phương án Thanh vừa từ chối. Tuyên bố đó không hợp lệ, đã bị bắt và sửa lại đúng bản Human Readable. Bài học: tuyên bố Freeze/PASS từ AI cần đối chiếu lại đúng quyết định gốc của Reality Owner trước khi tin, không chỉ tin vì AI tự tin.

### Discovery Open còn lại (không chặn gì)

- SessionRAM Persistence (🔴, chưa test A-D).
- ✅ Compiler Automation — **ĐÃ ĐÓNG (2026-08-04)** — xem Mục II.5 và [[ADR-022 Log as Input Buffer with Leader-Triggered Orchestration]].
- Snapshot Metadata Ownership (🟡, Open một phần — xem Freeze ở trên) — 3 field còn lại chưa Freeze.
- Truth Mapping V1 (⚪) — taxonomy 3-Truth: Working/Reference/Decision + quan sát "State" bị overload 3 nghĩa.
- Hierarchy of Prevention (⚪) — case Roaster Cashew, mới 1 case, chưa đủ để thành Principle.
- Boundary Formula vs Engine (🟡) — chưa có Reality Test nào chạy.

## II. PRODUCTION_COMPILER — đã sửa và xác nhận PASS

### Bug gốc (đã sửa)

`PRODUCTION_COMPILER` (công thức `=LET(...)` ở ô A1, KHÔNG phải Apps Script) từng emit Item Code dòng YIELD = full SKU (`FINAL-CKE-TIR-001-10cm-BDAY`) thay vì Base Code (`FINAL-CKE-TIR-001`) mà `ALL_ITEM` chỉ track theo Base Code → `VALIDATE_FAIL_INVALID_ITEM`. Đã sửa (dùng `base_code` thay `full_sku` cho Item Code + Location YIELD).

### Phát hiện lớn hơn — PREP_MASTER chứa 2 bản chất khác nhau

- Inventory Prep (STANDALONE): có tồn kho thật, để tủ dùng qua nhiều ngày (Steam Pumpkin, Cooked Rice, Soaked Cashew...).
- Recipe Module (INLINE): không tồn kho, luôn làm-dùng-hết trong ngày, chỉ chia nhỏ công thức (Tiramisu Crust, Filling các loại...).

Đã thêm cột `Execution Type` vào `PREP_MASTER` (giá trị STANDALONE/INLINE), Thanh đã tự tay Audit xong toàn bộ ~70+ dòng.

Đã sửa `PRODUCTION_COMPILER`: Component STANDALONE → giữ hành vi cũ (1 dòng CONS trỏ Prep). Component INLINE → nổ 1 tầng xuống RAW qua `UNIVERSAL_RECIPE_DETAIL`, không ghi movement Prep. Chỉ nổ đúng 1 tầng (không đệ quy) — giữ Rule 20 (V7, Local Explosion Only) cho Inventory Object, chỉ mở ngoại lệ có kiểm soát cho INLINE.

### Sự cố phát sinh trong lúc sửa (đã xử lý xong)

1. Bug ADR-016 lặp lại ở Production (`status_flag <> "OK"` lọc bỏ batch) — đã sửa (bỏ điều kiện lọc).
2. INLINE lồng INLINE có thật (Dark Filling chứa Coffee Shot/Pressed Tofu, cả 2 đều INLINE) — không nổ tiếp theo thiết kế (giới hạn 1 tầng), có cơ chế cảnh báo.
3. Cảnh báo `NESTED_INLINE_*` — Gemini ban đầu âm thầm xóa cảnh báo để tránh vỡ Validate; đã yêu cầu sửa lại: tách cảnh báo ra khỏi cột A:I (luồng Staging đọc), đưa sang cột N (vùng phụ). Đã xác nhận qua ảnh: cột A:I sạch, cột N chứa cảnh báo NESTED_INLINE.
4. False positive ban đầu (logic cảnh báo bắt cả Prep lồng STANDALONE) — đã sửa, giờ chỉ cảnh báo khi Prep lồng thực sự là INLINE.

### ✅ Full pipeline PASS — xác nhận qua kiểm tra thực tế

Đã kiểm lần lượt theo luồng PRODUCTION_COMPILER → TRANSACTION_STAGING → INVENTORY_LEDGER → STOCK_POSITION:

- Compiler routing STANDALONE/INLINE đúng, không còn coi mọi Prep là Inline.
- Không còn NESTED_INLINE tràn bảng (false positive đã hết).
- Ledger mirror đúng Compiler (không đổi Location/Qty).
- Stock Position cộng dồn đúng theo (Item Code, Location) — ví dụ `PREP-NUT-SOC-001` = -2.340,40g.
- Không có dấu hiệu recursion mở nhầm ngoài ý muốn.

### ❌ CLOSED — False Alarm: "RAW-OIL-VCO-001 không tự sum"

Ban đầu nghi ngờ `RAW-OIL-VCO-001` trong batch `DN-ASSM-260612-001` không được gộp lên `STOCK_POSITION`. Đã điều tra theo 3 giả thuyết:

1. Item Code lệch ký tự ẩn — loại (đối chiếu trực tiếp `UNIVERSAL_RECIPE_DETAIL`, khớp tuyệt đối).
2. Unit không khớp — loại (cả hai dòng CONS đều `g`).
3. Location khác nhau thật — loại (cả hai dòng CONS đều `DN_RAW`).

Kết luận: đọc nhầm tầng dữ liệu. `PRODUCTION_COMPILER`/`INVENTORY_LEDGER` ghi từng dòng CONS riêng theo Component (giữ traceability), không sum ở tầng này — đúng thiết kế. Gộp thật sự xảy ra ở `STOCK_POSITION`: `RAW-OIL-VCO-001` = -1.061,76 g, một dòng duy nhất, cộng dồn đúng tất cả batch. Không có bug. Đóng, không cần sửa.

## II.5 PREP_COMPILER — dòng thời gian & trạng thái thật

- 2026-07-13 (trước Sprint): 1 trong chuỗi compiler của Inventory Engine (`PREP_LOG → PREP_COMPILER, PRODUCTION_COMPILER → TRANSACTION_STAGING → INVENTORY_LEDGER`), đang build tuần tự.
- 2026-07-14 — Bug thật, đã sửa, thành ADR-016: `IF(status_flag <> "OK"; acc; ...)` âm thầm lọc batch WARNING trước khi sinh Transaction. Đã sửa: bỏ điều kiện lọc. Đúc kết ADR-016 (trích [[FOUNDATION-007 - What is Reality]]: Compiler không được quyền im lặng bỏ qua sự kiện đã thật sự làm thay đổi Reality; Status Flag chỉ để định tuyến, không để triệt tiêu). ADR ghi rõ: chưa lên Constitution, mới kiểm chứng đúng 1 Compiler.
- 2026-07-17→19 — Bug lặp lại đúng như ADR-016 dự đoán, ở `PRODUCTION_COMPILER`. Xác nhận đúng cảnh báo ADR-016. Đã sửa theo cùng cách.
- Bằng chứng Operational Validation của PREP_COMPILER: `Compiler Never Silences Reality` đã merge và test; Validation Layer chạy end-to-end; `Allow Unknown Output` test trên batch PREP; LOW YIELD → WAIT_REVIEW → Approved → Ledger; `INVENTORY_LEDGER` đã nhận batch PREP thật (ví dụ `DN-PREP-260712-07`); `effectiveQty` không mutate Snapshot.

Kết luận: PREP_COMPILER đã sửa đúng 1 bug thật, Frozen về mặt xử lý dữ liệu — khác với Architecture Review sâu (Execution Type routing, INLINE/STANDALONE, nested INLINE, Ledger mirror, Stock Position, Cost routing, BOM expansion) mà PRODUCTION_COMPILER vừa trải qua. Chưa có bằng chứng PREP_COMPILER đã qua vòng review kiến trúc cùng mức độ đó.

### ⚠️ Bằng chứng thật mới (2026-07-25) — kích hoạt lại câu hỏi PREP Compiler Architecture Review

Không còn thuần lý thuyết: case **Oat Coconut Pancake** lộ ra `PREP_COMPILER` hiện tại **không recursive explode** — khi PREP_LOG ghi "Oat Coconut Pancake Base 2.500g", Compiler chỉ sinh `+2.500g Base (YIELD)` / `-2.500g Batter (CONS)`, dừng ngay ở Batter, không nổ tiếp xuống RAW dù `UNIVERSAL_RECIPE_DETAIL` có đủ graph. Tức `Execution Type = INLINE` hiện tại chỉ là metadata khai báo, **chưa thật sự điều khiển hành vi Compiler** ở tầng PREP-làm-từ-PREP.

Đây là cùng 1 nguyên lý Consumption Resolver đã chốt cho Waste Engine ([[WASTE-ENGINE-CHECKPOINT-2026-07-22]] Mục 5) — xuất hiện lần thứ 3 độc lập (Production Compiler STANDALONE/INLINE gốc → Waste Consumption Resolver → giờ PREP_COMPILER prep-làm-từ-prep). Củng cố đây là Principle cross-cutting thật, không phải fix cục bộ từng nơi.

**Câu hỏi Reality cần Thanh trả lời trực tiếp** (quyết định đây có phải bug hay không): Khi làm Pancake Base, nhân viên có ghi `PREP_LOG` riêng cho `Oat Coconut Pancake Batter` (mỗi lần blend) trước khi cook thành Base không?

- Có ghi riêng → Batter là Inventory Object thật, Compiler dừng đúng theo nguyên lý Resolver. Không phải bug.
- Không ghi riêng → Ledger đang trừ tồn kho ảo cho Batter chưa từng tồn tại. Bug thật, ảnh hưởng Ledger Truth — đúng ngưỡng "bug lớn".

**✅ Đã trả lời — Reality xác nhận (2026-07-25): 2 vấn đề cùng lúc, không phải 1:**

- **Data bug (giao Gemini):** `PREP-BAS-OCP-001` (Oat Coconut Pancake Base) đang gắn sai `Execution Type = INLINE`, thực tế phải là STANDALONE — mỗi sáng làm ~20 phần, có `PREP_LOG`, giữ tồn, bán không hết thì cấp đông dùng tiếp hôm sau (carry-over thật). Còn `PREP-BAT-OCP-001` (Batter) đúng là INLINE thật — không log riêng, không tồn kho độc lập, luôn nổ khi dùng.
- **Compiler gap (chưa sửa, cần khóa scaling trước):** `PREP_COMPILER` hiện tại lấy thẳng `ing_codes`/`ing_qty` từ `UNIVERSAL_RECIPE_DETAIL`, không hề tra `Execution Type` — nên luôn dừng 1 tầng bất kể ingredient là STANDALONE hay INLINE. Đây là lỗ hổng thật, không phải thiết kế có chủ đích.

Quy tắc tổng quát đã khóa (không phải "nổ tới RAW", mà **"nổ tới Inventory Object gần nhất"**):

```
Ingredient RAW              → consume → STOP
Ingredient SEMI             → consume → STOP
Ingredient PREP/STANDALONE  → consume → STOP
Ingredient PREP/INLINE      → explode UNIVERSAL_RECIPE_DETAIL → recurse
```

Case Syrup (nằm trong Batter, nếu Syrup là STANDALONE) minh họa: traversal dừng tại Syrup, không nổ tiếp xuống RAW của Syrup — vì RAW đó đã bị consume lúc Syrup được sản xuất, nổ tiếp sẽ double-consume.

Materialization Boundary cho PREP = `Execution Type` (không cần bảng/field mới) — nhưng cùng caveat: Execution Type là signal thiết kế (declared), độ chính xác phụ thuộc `PREP_LOG` có được ghi đúng thật hay không (đúng tinh thần case Pressed Tofu ở [[Discovery — Hierarchy of Prevention]]). Chính bug Base/INLINE vừa tìm ra bằng cách hỏi Reality — minh chứng sống cho việc cần audit định kỳ đối chiếu Execution Type với Ledger thật, không tin tuyệt đối vào field khai báo.

⚠️ **Chưa Freeze — công thức Nested Scaling còn treo:** `Nested Multiplier = Required Qty / Standard Usable Yield` (không phải `Reference Batch Qty`). Case Pancake (`Reference=260, Yield=250, Required=250` → hệ số 1) chưa đủ phân biệt 2 mẫu số vì Required trùng khớp Yield. Cần thêm 1 case INLINE có `Required ≠ Yield` để xác nhận mẫu số đúng là Standard Usable Yield trước khi viết formula recursive thật.

> Ghi chú xác minh: nội dung Thanh dán có nhắc "cùng caveat đã ghi cho Assembly Cost" — chưa tìm thấy ghi chép nào về "Assembly Cost" trong vault hiện tại, có thể thuộc khung chat khác chưa relay. Không bịa thêm nội dung đó, chỉ ghi lại caveat đã nêu trực tiếp ở đây.

### Current Assessment

```
1. Functional Validation      ✅ Closed      — compile PREP_LOG → Staging đúng, bug status_flag đã sửa (ADR-016).
2. Technical Debt             🟡 Open        — chưa xác định cụ thể, không ảnh hưởng nguyên lý.
3. Compiler Automation        ✅ Đã đóng    — xem chi tiết ngay dưới đây, ADR-022.
4. Hierarchy of Prevention    ⚪ Discovery   — case Roaster Cashew, mới 1 case, PREP_COMPILER chỉ đọc Snapshot, không phải lỗi Compiler.
5. Compiler Contract          ⚪ Candidate   — chưa có "Compiler Contract" gom Input/Output/Không-được-làm như Production Compiler vừa làm.
```

### ✅ Compiler Automation — ĐÃ ĐÓNG (2026-08-04), Reality ép buộc quyết định

`PREP_LOG`/`WASTE_LOG` phình to không kiểm soát do Leader đã thực tế bắt đầu nhập liệu liên tục, không tự dọn, không ghi đè được. Quyết định đầy đủ đã Freeze thành [[ADR-022 Log as Input Buffer with Leader-Triggered Orchestration]]. Tóm tắt:

```
LOG ≠ nơi lưu lịch sử lâu dài
LOG = INPUT BUFFER (vùng nhập liệu tạm thời)

Trigger = Leader-triggered orchestration (nút bấm), KHÔNG phải:
  - Engine tự động hoàn toàn (Straight-Through Processing tuyệt đối)
  - Chờ Bếp trưởng kéo Snapshot thủ công (mô hình cũ)
```

**Luồng mới:** Leader nhập → bấm nút → Lock + xác định Event ID → Compile Snapshot (memory) → Validate → Risk Routing → Commit Ledger/Event Registry → Readback Verify → **Clear input đã Commit khỏi Log** (Log trở lại trống cho mẻ tiếp theo).

**Risk-based, không phải STP tuyệt đối:**
```
NORMAL   → hệ thống tự Validate → Auto Commit → clear Log
WARNING  → Leader xác nhận (ghi Approved By/Approved Time) → Commit → clear Log
CRITICAL → Auto Reject, giữ nguyên data tại Log, hiển thị lỗi
```
`Approved By`/`Approved Time` đã tồn tại thật trong luồng `WAIT_REVIEW` — không xóa Approval, chỉ đổi ai chịu trách nhiệm theo mức rủi ro.

**Nguyên tắc kỹ thuật đã khóa (xem đầy đủ ở ADR-022):**
- `Processing Status` chỉ phục vụ UX — không phải cơ chế chống duplicate chính. Idempotency thật dựa trên Event ID + duplicate check + Document Lock + Atomic Commit + Readback verification.
- Không tái sử dụng `Status Flag` (Risk Routing) làm `Processing State` — 2 trục khác nhau.
- Đơn vị xử lý là Event (1 YIELD + N CONS), Commit toàn bộ hoặc không Commit gì.
- Trước khi cho phép Clear, phải audit `EVENT_REGISTRY` đã giữ đủ thông tin audit chưa.
- Clear chỉ khi ĐỒNG THỜI: Validation=PASS, Commit=SUCCESS, Ledger duplicate check=PASS, Readback verification=true, Event Registry write=verified. Không xóa row vật lý, chỉ clear nội dung ô.
- Không viết lại Compiler hiện có — chỉ thêm lớp điều phối bên trên (`processPrepEventOneshot()`, `processWasteEventOneshot()`).

**Việc cần làm trước khi viết nút:** audit `PREP_LOG + EVENT_REGISTRY + Generate Snapshot boundary hiện tại` — xác nhận không vô tình xóa mất dữ liệu audit sau khi Commit. Chưa build.

**✅ Chính thức kick off — Phase D.7 (2026-08-05):**

```
D.7A Read-only audit ← đang chạy
D.7B One-click processing contract
D.7C PREP_LOG pilot implementation
D.7D PREP fault injection + live test
D.7E WASTE_LOG implementation
D.7F Cross-log acceptance + Freeze
```

Không viết lại Compiler, không đụng Phase D.6C đã Freeze (xem [[WASTE-ENGINE-CHECKPOINT-2026-07-22]], FREEZE RECORD). Làm `PREP_LOG` trước, chỉ áp dụng sang `WASTE_LOG` (D.7E) sau khi pattern đã chứng minh chạy đúng — không viết 1 generic engine cho mọi Log trước khi biết chắc 2 luồng thực sự tương đồng.

`D.7A` audit read-only bao quát: schema/formula thật của `PREP_LOG`/`WASTE_LOG`, toàn bộ pipeline hiện tại (Compiler→Staging→Validate→Commit→Ledger→Event Registry→Error Log), inventory toàn bộ Apps Script function đọc/ghi các sheet liên quan, Event Atomicity (có Partial Commit không), Idempotency (phân loại `SUFFICIENT_FOR_BUTTON`/`PARTIAL`/`MISSING`, không mặc định dùng Processing Status làm cơ chế chính nếu Event/Ledger đã có duplicate guard), Safe Clear Contract (cột nào được phép Clear, cột nào không), và Button Selection Model (3 phương án, đánh giá risk từng cái). Chưa viết code, chưa sửa dữ liệu.

**✅ Cập nhật tiến độ (2026-08-07) — D.7C (PREP Event → Inventory) đã tiến xa, chi tiết đầy đủ ở [[D7C-PREP-INVENTORY-CHECKPOINT-2026-08-07]]:**

```
D.7C-1C Recipe Resolution              CLOSED / PASS
D.7C-1D Stock Movement Package         CLOSED / PASS
D.7C-1E-0 Package → Staging preview    CLOSED / PASS
D.7C-1G Source Event Finalization      🚩 Evidence Gap — báo CLOSED/PASS nhưng thiếu raw evidence,
                                           CHƯA dùng làm dependency chắc chắn cho phase sau
```

Phase D.7C sinh ra Rule mới quan trọng: [[Governance-Protocol]] Mục 5 — IT Decision Authority (không bắt Thanh quyết chi tiết kỹ thuật thuần IT, Claude/GPT tự quyết/tự đóng nếu không đổi ý nghĩa nghiệp vụ đã chốt). Next action: chạy read-only `test_D7C_1G_FinalizationEvidenceAudit()` để đóng đúng Evidence Gap trước khi coi D.7C-1G là nền vững cho phase kế tiếp.

**✅ Cập nhật tiến độ (2026-08-09) — D.7C-3 (WASTE_LOG Button Orchestration) đã bắt đầu, nhân bản pattern từ D.7C PREP_LOG, chi tiết đầy đủ ở [[D7C3-WASTE-BUTTON-ORCHESTRATION-CHECKPOINT-2026-08-09]]:**

```
D.7C-3A-2  Waste Identity + Domain Router Build   CLOSED / PASS
D.7C-3B    Waste Logic Orchestrator               CLOSED / PASS
D.7C-3C    Bind Button + Operator Feedback        PASS (build) / IN PROGRESS (data)
```

3/11 event Waste thật đã hoàn tất trọn vòng đời (archive+clear+finalize) không double-write. 3 event khác đang bị chặn `UNIT_CONVERSION_FAILED` (1 code bug canonicalization + thiếu Conversion Rule) — dừng đúng lúc trước khi ghi sai, chưa chạm Staging. **Đang chờ chạy `D.7C-3C-INCIDENT-PATCH-3`** (đã AUTHORIZED, chưa có kết quả) trước khi tiếp tục Batch cho các event còn lại.

## II.6 Discovery — Boundary Formula vs Engine

Question: Đâu là ranh giới tối ưu giữa Google Sheets Formula và Apps Script Engine cho các Compiler (PREP/PRODUCTION/SALES/WASTE)?

Reality Test: Benchmark Generate với quy mô dữ liệu tăng dần (100, 500, 1000... Event), đo Performance và Maintainability trước khi quyết định chuyển bất kỳ Compiler nào.

Status: 🟡 Discovery — chưa Freeze. Không giả định hướng nào tốt hơn — để Reality Test quyết định. Mỗi Compiler có thể ở boundary khác nhau.

## II.7 Waste Engine — đã tách ra file riêng

Toàn bộ nội dung Waste Engine (bug Unit, Discovery Business Object/`inline_yield`, thiết kế dropdown, Discovery "Multi-Domain Reader") đã chuyển sang [[WASTE-ENGINE-CHECKPOINT-2026-07-22]] để giữ file chính gọn. Waste Engine vẫn chưa phải Mission chính thức đang mở — chỉ đang điều tra thiết kế trước khi build (`WASTE_LOG` đã tồn tại, `WASTE_COMPILER` chưa có).

## III. Còn mở — chưa phải Blocker

🟢 Mission "Production Compiler ổn định" — **đóng** ở trạng thái Operationally Stable (2026-07-22). Không hoàn hảo — còn Discovery/Technical Debt — nhưng không còn Blocker kiến trúc, không có bug đe dọa Transaction Truth/Ledger.

🔵 Mission tiếp theo (mở 2026-07-22): **"Reality Validation"** — không thiết kế thêm, không refactor, không thêm Rule. Mục tiêu: chạy Compiler hiện tại qua nhiều case PREP thật đa dạng (INLINE/STANDALONE/PASSIVE), so sánh output với vận hành thực tế. Chỉ mở "PREP Compiler Architecture Review" nếu xuất hiện pattern lặp lại — không mở ngay vì lý do kiến trúc thuần túy.

Thứ tự: `Production Compiler (Stable) → Reality Validation → [nếu có Pattern] → Prep Compiler Architecture Review`

- `inline_yield` — toàn bộ nội dung đã chuyển sang [[WASTE-ENGINE-CHECKPOINT-2026-07-22]] (Mục 2), vì đây hiện là shared dependency chủ yếu phục vụ thiết kế Waste Engine. Không mở lại Mission Production Compiler để giải quyết — Compiler vẫn giữ Operationally Stable. Tóm tắt: câu hỏi gốc đã sửa từ "Batch hay Yield Qty" (sai khung) sang "Business Object nào" — đã đóng phần lớn bằng dữ liệu thật (`FINAL_PRODUCT_MASTER` có sẵn Business Rule), còn 1 nhánh phụ (Portion Size — 2 tầng độc lập với Yield Qty) đang dùng cho thiết kế dropdown Waste, xem file riêng.
- `LEFT(c_code;4)` trong Compiler — technical debt, chưa dọn.
- INLINE lồng INLINE nhiều tầng có thật — bằng chứng vận hành thật (4 case NESTED_INLINE trong `PRODUCTION_COMPILER`, 19-22/06, 3 nhân viên khác nhau, loại trừ lỗi thao tác cá nhân). Compiler chỉ nổ đúng 1 tầng rồi dừng, cảnh báo `NESTED_INLINE_*` ở cột N.

### Case cụ thể đã soi (2026-07-22) — cả 2 đóng ở Open Discovery, không Freeze

- **`PREP-PRO-PTO-001` (Đậu hũ ép)** — Reality xác nhận: ép qua đêm, có thời gian chờ, đúng bản chất STANDALONE. Vấn đề là Business Process, không phải Compiler: chưa ghi `PREP_LOG` thật. Roadmap (ngoài Mission hiện tại): Training SOP ghi PREP_LOG → có log thật → mới đổi Execution Type → Compiler dùng STANDALONE. **Không đổi Execution Type trước khi có log thật** — đảo thứ tự sẽ gây Stock Position âm giả.
- **`PREP-COF-COS-001` (Coffee Shot)** — Execution Type = Unknown (hợp lệ), vì Consumer chưa tồn tại (Drink Engine chưa build). Không ép phân loại khi chưa có Consumer thật.

Pattern rút ra: không phải RAW/PREP/INLINE/STANDALONE quyết định Architecture — mà **`Consumer Requirement → Execution Model → Execution Type`.**

```
Discovery:      ✓ Đã biết từ trước, nay có bằng chứng vận hành thật
Mechanism:      ✓ Flag NESTED_INLINE_* hoạt động đúng
Status:         OPEN EXPERIMENT — KHÔNG PHẢI BUG, KHÔNG CẦN ADR
```

Đóng chủ đề này cho tới khi: mở Mission Drink Engine, hoặc Mission Execution Type Review.

## IV. Discovery mới — chưa Frozen, cho Layer 3 (Transaction Engine, chưa bắt đầu build)

Nguồn: 2 file thực tế — `MasterDocument (5).xlsx` (Sales Detail) và `MasterDocument (6).xlsx` (Purchase Detail).

- Sales Report: có Mã chứng từ, Ngày, Khách, Bàn, Tổng tiền — không có Item Code nguyên liệu. POS chỉ biết "bán 1 Tiramisu", không biết BOM bên dưới → luồng Sales Consumption bắt buộc phải đi qua Compiler (BOM), giống cơ chế Production hiện tại.
- Purchase Report: có Mã chứng từ, Ngày, NCC, Tổng tiền — không có logic BOM nào cần thiết, đi thẳng vào Ledger.

Đề xuất phân loại (CHƯA FREEZE):

```
POS EXPORT → INPUT LOG → COMPILER (nếu cần) → TRANSACTION → LEDGER
```

- Nhóm A — Direct Transaction (không cần BOM): Purchase, Waste, Transfer, Adjustment, Opening Balance.
- Nhóm B — Compiled Transaction (phải qua Compiler): Prep, Production, Sales Consumption.

Nhất quán với triết lý hiện có (chỉ nguồn có BOM mới cần Compiler — giống STANDALONE/INLINE ở Production Compiler). Transaction Engine chưa bắt đầu build (xem Mục V) → phân loại này giữ ở Discovery, cần Reality Test (thử áp vào ít nhất 1 nguồn mỗi nhóm) trước khi khóa cứng.

## V. Việc chưa bắt đầu (ngoài phạm vi Mission hiện tại)

- Wiring `Read_Infrastructure.gs` vào main Engine.
- Resolving SessionRAM Persistence definitively với Reality evidence.
- Pending ERP modules: Waste Engine (đang điều tra thiết kế, xem [[WASTE-ENGINE-CHECKPOINT-2026-07-22]], chưa chính thức mở Mission build), Transfer Engine, Audit Engine, Workshop Engine, Branch Consumption, Sales Engine, POS Integration.
- Meta-Architecture (Sprint O4 tentative) — chưa Frozen, chờ Reality validation.
- Python tooling tại `D:\kurumi\Kurumi_V8\python` cho tự động hóa đọc vault.

## VI. Quy ước quan trọng cần khung mới biết

- Phạm vi sửa Compiler luôn là công thức Google Sheets (`=LET(...)` ở ô A1 các sheet `*_COMPILER`), KHÔNG phải Apps Script `.gs`. Engine (`.gs`) chỉ đọc dữ liệu đã có sẵn trong `TRANSACTION_STAGING`, không biết gì về `PREP_MASTER`/`Execution Type`.
- Phân biệt vai trò các tầng: `PRODUCTION_COMPILER`/`INVENTORY_LEDGER` = ghi chi tiết từng dòng theo Component (không sum, giữ traceability). `STOCK_POSITION` = tầng gộp duy nhất, cộng dồn theo (Item Code, Location). Đừng nhầm "không sum ở Ledger" là bug.
- Knowledge Creation Rule: không viết ADR/tài liệu chính thức cho quyết định INLINE Explosion (recursive hay không) cho tới khi chạy thật + PASS đủ lâu.
- Mission Boundary: chỉ giải quyết đúng 1 việc tại 1 thời điểm — hiện tại là **"Reality Validation"** (xem Mục III), không phải "Production Compiler ổn định" nữa (đã đóng). Chưa mở WASTE_COMPILER hay Mission nào khác — Mục II.7 chỉ là 1 Discovery/taxonomy quyết định sớm, không phải Mission đã mở.
- Toàn bộ file Spec/Mission Brief đã gửi Gemini nằm trong lịch sử chat khung cũ — không có trong vault chính thức (file tạm dùng giao việc).

## VII. Câu hỏi đang treo — chờ Thanh trả lời trực tiếp

1. Mission "PREP Compiler Architecture Review" — mở Mission thì **chưa mở**, chờ Reality Validation cho ra Pattern trước (Mục III). Nhưng xem mục 5 dưới đây — 1 case cụ thể vừa cần Thanh trả lời trực tiếp, không cần đợi mở Mission.
2. [[Discovery — Snapshot Metadata Ownership]] — field `Snapshot ID` đã Freeze ([[ADR-017 Snapshot ID Format]]). 3 field còn lại (`Compiler Source`, `Generated By`, `Generated Time`) — Proposal cũ chưa đủ Reality để Freeze, cần thêm bằng chứng.
3. [[Discovery — Boundary Formula vs Engine]] — chưa chạy Reality Test benchmark nào.
4. `Team-Hierarchy-Unified.md` — vẫn chưa nhận được nội dung đầy đủ (bản 2-Layer, Cross-Team Principle "Evidence Before Authority", Discovery Independence) — còn treo từ trước.
5. ✅ **Đã đóng (2026-07-25)** — case Oat Coconut Pancake (xem Mục II.5): 2 vấn đề, không phải 1 — data bug thật (`PREP-BAS-OCP-001` gắn sai INLINE, phải là STANDALONE, giao Gemini sửa) + Compiler gap thật (`PREP_COMPILER` không tra `Execution Type` khi nổ nested). Quy tắc "nổ tới Inventory Object gần nhất" đã khóa cho PREP. Còn treo: công thức Nested Scaling (`Required Qty / Standard Usable Yield`) chưa đủ case để xác nhận.

## Appendix — Audit Trail: RAW-OIL-VCO-001 False Positive

```
Observation
    ↓
Hypothesis A — Item Code lệch ký tự ẩn
    ↓ Rejected (đối chiếu trực tiếp UNIVERSAL_RECIPE_DETAIL, khớp tuyệt đối)
Hypothesis B — Unit không khớp
    ↓ Rejected (cả hai dòng CONS đều "g")
Hypothesis C — Location khác nhau thật
    ↓ Rejected (cả hai dòng CONS đều DN_RAW)
Conclusion — Read wrong data layer
    Ledger/Compiler không sum theo đúng thiết kế (giữ traceability theo Component:
    CONS-1-x, CONS-2-x, CONS-3-x), bị đọc nhầm thành lỗi "thiếu sum".
    STOCK_POSITION — tầng gộp thật sự — đã tổng hợp đúng theo (Item Code, Location):
    RAW-OIL-VCO-001 = -1.061,76 g, một dòng duy nhất.
```

Status: CLOSED — False Positive.
